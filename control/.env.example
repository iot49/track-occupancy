# DNS: Add a wildcard A record in Cloudflare: *.rails49.org → <server-LAN-IP>
# This allows Traefik to obtain a wildcard TLS cert via DNS-01 challenge,
# and makes all *.rails49.org subdomains resolvable on the local network.

RAILS_DOMAIN=rails49.org
CF_DNS_API_TOKEN=your-cloudflare-api-token-here
CF_API_EMAIL=your@email.com
TZ=Europe/Berlin

# Track-occupancy detector settings
# CAMERA_URL=http://192.168.1.x/snapshot.jpg
# INTERVAL_MS=1000
